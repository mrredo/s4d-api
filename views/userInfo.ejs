<%- include('partials/header') %>
<html>
<body>
    <% if(user) {%>
        <div class="userinfo">
            <img id="avatarP" width="100" height="100" src=<%= user.avatarURL %>></img>
            <p id="username">Hello <%= user.username %>#<%= user.discriminator %> <br>
            <p id="apiKey">Your api key: ********************</p>
            <div class="center">
                <button id="keybtn">Copy key</button><button id="hidebtn">Unhide</button>
            </div>
            </p>


        </div>
        <style>
            @import url('https://fonts.googleapis.com/css2?family=Roboto&display=swap');
            #avatarP {
                border-radius: 100px;
                border-color: black;
                border-style: solid;
                display: block;
                margin-left: auto;
                margin-right: auto;   
                margin-top: 10px;
            }
            #username {
                text-align: center;
            }
            #apiKey {
                text-align: center;
            }
            #keybtn {
                margin-left: auto;
                margin-right: auto;  
            }
            .userinfo {
                border-style: solid;
                margin-top: 20px;
                font-family: 'Roboto', sans-serif;
            }
            .center {
                margin-left: 600px;
                align-items: center;
            }
        </style>
    <% } else { %>
        <a href="/auth/login">Login</a> to see your info!
    <% } %>
</body>
</html>